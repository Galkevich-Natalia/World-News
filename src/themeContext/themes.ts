import { Colors, ThemesType } from "./types";

export const Themes: ThemesType = {
    light: {
        body: Colors.WHITE,
        color: Colors.BLACK,
        backgroundForm: Colors.TRANSPARENT
    },

    dark: {
        body: Colors.DARKGREY,
        color: Colors.WHITE,
        backgroundForm: Colors.WHITE
    }
}
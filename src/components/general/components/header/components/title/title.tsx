import {  AppTitle, TitleContainer } from "./styledTitle";

export const Title = () => {
    return (
        <TitleContainer>
            <AppTitle>World News</AppTitle>
        </TitleContainer>
    );
};
import { styled } from "styled-components";

export const ContBtnBurgerMenu = styled.div`
    display: none;

    @media screen and (max-width: 640px) {
        display: flex;
        color: white;
        font-size: 35px;
        padding: 10px 0;
        cursor: pointer;
`;